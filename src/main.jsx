import React from 'react'
import ReactDOM from 'react-dom/client'
import { PerrosApp } from './PerrosApp'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PerrosApp/>
  </React.StrictMode>
)
